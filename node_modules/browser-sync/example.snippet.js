var browserSync = require("./index");

//browserSync.use({
//    plugin: function () { /* noop */},
//    hooks: {
//        'client:js': require("fs").readFileSync("./reloader.js", "utf-8") // Link to your file
//    }
//});

browserSync({
    scriptPath: function (script) {
        return script;
    }
});